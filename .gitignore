# Student material / generated creds
student-*
*-creds.json

# Terraform working directory
.terraform/

# Provider lockfile (kept ignored to match previous repo behavior)
.terraform.lock.hcl

# Terraform state (never commit)
**/*.tfstate
**/*.tfstate.*
.terraform.tfstate.lock.info

# Terraform variables (often contain secrets)
*.tfvars
*.tfvars.json

# Terraform crash logs
crash.log
crash.*.log